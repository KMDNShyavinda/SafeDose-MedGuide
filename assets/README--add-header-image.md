Place your photographic header image here to use as the homepage wallpaper.

Steps:
1. Save the image file you provided as `header.jpg` (JPEG) inside the `assets/` folder: `assets/header.jpg`.
2. The site will automatically use `assets/header.jpg` in the header when present. If not present the SVG wallpaper will be used as a fallback.

Notes:
- Recommended size: 1600x900 or wider. Keep file size small (< 400KB) to preserve load performance.
- If you'd like me to add the actual image file into the repo, please upload it here or allow me to access the attachment's binary.
